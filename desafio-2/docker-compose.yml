

services:
  nginx-desafio:
    container_name: desafio-2
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - volume-desafio-2:/usr/share/nginx/html
volumes:
  volume-desafio-2:
    external: true
      
      

  